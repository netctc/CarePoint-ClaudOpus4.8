# Phase 20 Validation Reports

`phase20-static-preview-report.md` and `phase20-static-preview-report.json` were generated by applying Phase 17, Phase 18, Phase 19, and Phase 20 over the uploaded base workspace in this environment.

Result observed in the preview workspace:

```text
READY_WITH_WARNINGS
Passed: 49/50
Errors: 0
Warnings: 1
```

The single warning is the known Prisma major-version drift between the root package and the API workspace:

```text
root prisma=^7.6.0
root @prisma/client=^7.6.0
api prisma=^5.18.0
api @prisma/client=^5.18.0
```

Re-run `scripts/phase20/validate-phase20-merge.mjs` after applying the patch to your real local workspace because your branch may contain newer changes.
