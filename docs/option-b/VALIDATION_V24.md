# CarePoint Option B - Validation V24

Run `npm run check:secrets --silent`, `npm run verify:workspace --silent`, `npm run verify:python-worker --silent`, selected or full Python tests, and validate TypeScript in CI with `npm ci && npm run build:contracts && npm run build:api`.
