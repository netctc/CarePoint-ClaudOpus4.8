# Validation V44

Run `python -m compileall`, `PYTHONPATH=services/python-worker python scripts/option_b/verify_python_worker.py`, focused smoke checks for both V44 gates, `unzip -tq`, and CI/staging TypeScript builds with `npm ci`, `npm run build:contracts`, `npm run build:api`.
