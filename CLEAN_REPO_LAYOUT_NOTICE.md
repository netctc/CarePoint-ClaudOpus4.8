# Clean repository layout package

This corrected archive does not contain packaging-only `frontend/`, `backend/`, or `phase*_work/` root folders.

Copy/merge files according to the repository paths shown in the archive, for example `apps/admin/...` and `services/api/...`.

Do not copy the older package root folders into your project. If you already copied `frontend/` or `backend/` into the repository root, remove those folders after verifying no manual edits were added inside them.
